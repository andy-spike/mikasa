# OpenRouter through the AI SDK for all model calls

All model calls use the AI SDK with the OpenRouter provider. Mikasa uses fixed model and reasoning profiles for each feature, and the Learner never selects a model. OpenRouter gives one billing and routing boundary with provider fallbacks and data-collection controls, while the AI SDK supplies generation, agent, tool, message, and streaming interfaces.
