# Verify coding Lessons in an isolated sandbox

Mikasa runs executable examples from coding Lessons in a temporary Vercel Sandbox before publishing the Course. The sandbox receives no production secrets, and its file changes are temporary. We rejected publishing untested runnable examples because a model review alone cannot confirm that code executes.
